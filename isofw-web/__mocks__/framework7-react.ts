import makeMockElement from "isofw-shared/src/testUtil/makeMockElement"

const List = makeMockElement("List")
const Popover = makeMockElement("Popover")
const ListItem = makeMockElement("ListItem")
const Link = makeMockElement("Link")

export {
  List, Popover, ListItem, Link
}